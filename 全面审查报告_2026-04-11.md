# 四川非遗文旅数字导览小程序 - 全面逻辑审查报告

> 审查时间：2026-04-11
> 审查范围：全部页面组件、配置文件、云函数文档
> 审查结论：**0 阻断性错误，可安全上线**

---

## 一、今日已修复问题（2026-04-11）

| 文件 | 问题 | 状态 |
|------|------|------|
| App.vue | Vue is not defined（两个script块未合并） | ✅ 已修复 |
| pages.json | navigationBarHidden/navigationStyle无效字段；uni-popup全局注册异常 | ✅ 已修复 |
| manifest.json | navigateToMiniProgramAppIdList已废弃 | ✅ 已修复 |
| cloudFunctionNames.js | 缺少submitFeedback导出 | ✅ 已修复 |
| myCheckin.vue | 集合名硬编码`user_checkins` | ✅ 已修复 |
| 云函数文档 | getOpenId缺少wx-server-sdk引入 | ✅ 已修复 |

---

## 二、代码逻辑审查

### 2.1 配置中心化 ✅ 优秀

**config.js** 完整包含：
- 云环境配置（env、enabled）
- 腾讯地图Key（NV7BZ-KHFCV-S4SPF-5ULSS-OVVNT-D4FMA）
- 百度TTS配置（spd、pit、vol、per）
- 所有云函数名称（CLOUD_FUNCTIONS）
- 所有集合名称（COLLECTIONS）
- 云存储路径（STORAGE_PATHS）

**cloudFunctionNames.js** 统一导出：
- 云函数名称常量
- 便捷调用方法（cloudFn 对象）

### 2.2 页面逻辑审查

#### 首页（index.vue）✅
- 数据来源：直接查询 `swiper`、`heritage`、`scenic` 集合
- 降级处理：无数据时显示空状态
- 路径引用：正确

#### 探索页（guideIndex.vue）✅
- 统计加载：使用Promise.all并发查询
- 本地存储浏览历史
- 降级处理：失败时显示0

#### 非遗详情（heritageDetail.vue）✅
- 收藏检查：使用云函数检查（正确）
- 语音导览：优先用数据库audioUrl，降级用TTS
- 打卡跳转：参数正确传递

#### 景点详情（scenicDetail.vue）✅
- 逻辑与非遗详情类似

#### 打卡页（checkin.vue）✅
- 三步骤流程：定位 → 拍照 → 提交
- 图片上传：先上传到云存储
- 打卡提交：调用 cloudFn.submitCheckin()
- 海报生成：使用canvas绘制

#### 我的打卡（myCheckin.vue）✅
- 分页加载：page + pageSize
- Tab筛选：全部/非遗/景点
- 统计：并发查询三种类型的总数

#### 收藏页（favorites.vue）✅
- 调用 cloudFn.getFavoriteList()
- openid通过 getOpenId 云函数获取
- 取消收藏：调用 cloudFn.removeFavorite()

#### 路线选择（routeSelect.vue）✅
- 优先从云数据库 `routes` 加载
- 失败降级到本地4条预设路线
- 分类筛选：全部/热门/文博/自然/文化/民俗

#### AR识别（arRecognition.vue）✅
- 相机权限处理：检查+申请+提示
- 识别流程：拍照 → 上传 → 调用getARResult → 降级模拟
- AR_CONFIG.enabled = false，标记为待接入

#### 反馈页（feedback.vue）✅
- 调用 CLOUD_FUNCTIONS.submitFeedback
- 降级方案：云函数失败则直接写数据库

#### 个人中心（mine.vue）✅
- 静默登录：uni.login()
- 用户信息：open-type="getUserInfo"
- 统计数据：本地缓存 + 云端同步
- 退出清理：所有相关storage

### 2.3 云函数清单审查

#### 已配置云函数（前端引用）

| 云函数名 | config.js | cloudFunctionNames.js | 状态 |
|----------|-----------|----------------------|------|
| getOpenId | ✅ | ✅ | ⚠️ 需部署 |
| user-login | ✅ | ✅ | ⚠️ 需部署 |
| favorite-add | ✅ | ✅ | ⚠️ 需部署 |
| favorite-remove | ✅ | ✅ | ⚠️ 需部署 |
| favorite-list | ✅ | ✅ | ⚠️ 需部署 |
| submit-feedback | ✅ | ✅ | ⚠️ 需部署 |
| addCheckin/submitCheckin | ✅ | submitCheckin ✅ | ⚠️ 需部署 |
| getMyCheckin | ✅ | ✅ | ⚠️ 需部署 |
| checkLocation | ✅ | ✅ | ⚠️ 需部署 |
| tts-guide | ✅ | ✅ | ⚠️ 需部署+配置 |
| geo-reverse | ✅ | ✅ | ⚠️ 需部署 |
| getRoutes | ✅ | ✅ | ⚠️ 需部署 |
| getARResult | ✅ | ✅ | ⚠️ 需部署 |
| addBrowseHistory | ✅ | ✅ | ⚠️ 需部署 |

#### 云函数配置文档 ✅ 完整
所有云函数都有完整的 `index.js` 和 `package.json` 代码

---

## 三、待处理事项

### 3.1 必须完成（上线前）

#### 1. 部署所有云函数到微信云开发控制台
需部署的云函数（按优先级）：
1. **getOpenId** - 其他云函数的依赖
2. **favorite-add/remove/list** - 收藏功能
3. **submit-feedback** - 反馈功能
4. **addCheckin/submitCheckin** - 打卡功能
5. **getMyCheckin** - 打卡记录
6. **geo-reverse** - 逆地址解析
7. **tts-guide** - 语音导览（需配置百度API Key）
8. **getRoutes** - 路线列表
9. **getARResult** - AR识别（可选，功能标记为disabled）
10. **user-login** - 用户登录

#### 2. 配置 TTS 环境变量（可选）
- 百度AI开放平台申请语音合成权限
- 在 tts-guide 云函数配置环境变量：
  - `BAIDU_TTS_API_KEY`
  - `BAIDU_TTS_SECRET_KEY`

### 3.2 建议完成（提升体验）

#### 1. 创建云数据库集合和索引
在微信云开发控制台创建以下集合：
- `heritage` - 非遗数据
- `scenic` - 景点数据
- `swiper` - 轮播图
- `user_favorites` - 用户收藏
- `checkin` - 打卡记录
- `browse_history` - 浏览历史
- `feedback` - 用户反馈
- `routes` - 路线数据
- `users` - 用户信息

建议索引：
```
user_favorites: { _openid: 1, targetType: 1 }
user_favorites: { _openid: 1, targetId: 1 }
checkin: { _openid: 1, createTime: -1 }
checkin: { _openid: 1, targetType: 1 }
browse_history: { _openid: 1, browseTime: -1 }
```

#### 2. 填充初始数据
- 轮播图（swiper集合）
- 非遗数据（heritage集合）
- 景点数据（scenic集合）
- 推荐路线（routes集合）

#### 3. AR功能完善（可选）
- 申请腾讯云AI或百度AI图像识别服务
- 实现 getARResult 云函数
- 将 AR_CONFIG.enabled 改为 true

---

## 四、潜在问题与风险

### 4.1 低风险

| 问题 | 说明 | 影响 |
|------|------|------|
| copyright年份 | mine.vue显示 © 2024 | 可忽略 |
| 打卡位置验证 | checkLocation云函数未部署时，打卡不验证位置 | 用户可任意位置打卡 |
| TTS未配置 | 语音导览会返回失败提示 | 功能降级 |

### 4.2 无风险项

| 检查项 | 状态 |
|--------|------|
| Vue is not defined | ✅ 已修复 |
| 云函数名称不匹配 | ✅ config.js 与 cloudFunctionNames.js 一致 |
| 页面路径引用 | ✅ 所有路径正确 |
| 参数传递 | ✅ 详情页→打卡页参数正确 |
| 云存储路径 | ✅ 配置完整 |
| TabBar图标 | ✅ 4个tab图标都配置 |

---

## 五、上线前检查清单

### 5.1 云函数部署（必须）
- [ ] getOpenId
- [ ] user-login
- [ ] favorite-add
- [ ] favorite-remove
- [ ] favorite-list
- [ ] submit-feedback
- [ ] addCheckin（或submitCheckin）
- [ ] getMyCheckin
- [ ] geo-reverse
- [ ] tts-guide（可选）
- [ ] getRoutes

### 5.2 数据库配置（必须）
- [ ] 创建所有集合
- [ ] 创建必要索引
- [ ] 导入初始数据（轮播图、非遗、景点）

### 5.3 小程序后台（必须）
- [ ] 配置服务器域名（uploadFile合法域名）
- [ ] 配置业务域名（如有webview）
- [ ] 申请地图服务资质

### 5.4 测试验证
- [ ] 首页轮播图加载
- [ ] 非遗/景点列表加载
- [ ] 收藏功能（添加/取消）
- [ ] 打卡功能（拍照上传）
- [ ] 语音导览（如已配置TTS）
- [ ] 定位功能
- [ ] 反馈提交

---

## 六、结论

**项目整体质量：良好**

1. ✅ 代码结构清晰，配置集中管理
2. ✅ 云函数配置文档完整
3. ✅ 已修复所有阻断性错误
4. ✅ 降级策略完善（云端失败→本地兜底）
5. ⚠️ 云函数需部署才能完整使用
6. ⚠️ 初始数据需导入才能展示内容

**建议：按第五节清单完成云函数部署和数据库配置后，可安全上线。**

---

*报告生成时间：2026-04-11 13:35*
