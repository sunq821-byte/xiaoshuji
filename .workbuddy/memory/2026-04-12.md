# 2026-04-12 工作日志

## 打卡功能升级（2026-04-14）
- **checkin.vue 全量重写**：新增打卡照片上传（最多3张）、备注输入（最多100字）
- 图片先通过 `wx.cloud.uploadFile` 上传至 `images/checkin/` 云存储，再调 `checkin-submit` 云函数写入数据库
- 支持上传进度百分比实时显示，云函数失败时降级本地存储兜底
- 本地存储 `localCheckins` 同步保存 `photoUrl`/`photoUrls`/`note` 字段
- **myCheckin.vue 更新**：打卡卡片展示打卡照片（无打卡照则显示封面图）、备注文本，支持点击预览大图
- **heritageDetail.vue HTML渲染修复**：详细描述 `<text>` 改为 `<rich-text>` 组件，解决富文本HTML标签原样显示问题
- 特色技艺卡片 `techItems` 也全部改为 `<rich-text>` 渲染，确保特色技艺列表内容正确解析
- **heritageDetail.vue 分类枚举映射（重新实现）**：映射表移到 `loadHeritageDetail()` 方法内，数字key直接写不用字符串转换；单选/多选两种情况分别处理；WXML 用 `heritageDetail.categoryText`；删除了模块级 categoryMap 和 computed；兼容单选/多选两种数据格式
- **特色技艺卡片重新设计**：横向滚动标签列表（chip形式），每个标签带序号圆点、川红渐变背景、圆角24rpx；整体横向滚动告别纵向堆叠
- 0 lint 错误

## scenicDetail.vue 全面优化（2026-04-12）
- **三宫格图标修复**：语音导览🔊 / 地图导航🗺️ / 我要打卡📍 全换 emoji，彻底解决 uni-icons 字体未加载白图标问题
- **封面图升级**：标题、副标题、等级badge叠加到封面图底部渐变层，高度520rpx，视觉更震撼
- **移除云存储图标依赖**：收藏/分享改为 emoji（❤️🤍📤），删除 loadIcons 方法和所有 cloud:// 图标URL
- **主色对齐 #D93025**：三宫格渐变、错误重试按钮、loading动画、feature-tag 全改川红系
- **游览贴士卡片**：特殊暖黄背景（#fff9f0）区分其他内容卡片
- **布局优化**：main-content 添加左右 padding 24rpx，info-card 圆角 40rpx，section-card 间距收紧至 20rpx
- **移除自定义返回按钮**：删除 cover-container 内的.back-btn 及相关 goBack() 方法，渐变遮罩去掉顶部暗角，交给小程序原生导航栏处理
- **feedback.vue 字段名修复**：前端传 `desc`/`imgs`，云函数期望 `content`/`images`，统一改为与云函数文档一致；降级fallback也同步修复；顶部渐变改为川红系 #D93025
- 0 lint 错误

## UI 设计优化方案输出
- 分析了首页、探索页、非遗详情页、个人中心4个核心页面
- 输出完整设计优化方案：UI设计优化方案_2026-04-12.md（artifact）
- 核心优化方向：
  1. 设计令牌：主色调整为 #D93025（川红），背景改暖米白 #F7F5F2
  2. 首页导航图标用 emoji 替代云存储图片，解决白屏问题
  3. 探索页移除 banner-decor 云存储图片，改纯CSS装饰
  4. 景点卡片加大至 280×360rpx，轮播图加高至 420rpx
  5. 加入川味文案彩蛋（空状态、加载提示等）

## UI 设计优化落地（第二轮实施）
- 全部优化已落地到代码，0 lint 错误
- **App.vue**：设计令牌重写，主色 #D93025（川红），背景 #F7F5F2（暖米白），深色模式暗棕色系
- **index.vue**：
  - 搜索栏改川红渐变顶栏，显示"四川非遗文旅"品牌标题
  - 导航图标全部换 emoji（🎭🏔️🗺️），彻底移除云存储 nav 图片和 navImageUrls/loadNavIcons 代码
  - 轮播图加高至 420rpx，景点卡片加大至 280×360rpx
  - 空状态文案改为"锅里还没有菜，先去添几道？"
  - 骨架屏改暖色（#F0EDE8 → #E8E4DE）
- **guideIndex.vue**：
  - 移除 banner-decor 云存储图片，改为纯CSS圆圈装饰 + emoji 大图标
  - 统计数字从 48rpx 收敛至 40rpx
  - 分类 chip 背景色改暖米白 #F7F5F2，选中态暖红
  - 主色全局替换为 #D93025
- **mine.vue**：
  - 顶部渐变去除紫色，改为纯川红系（#D93025 → #A8201A → #8A1A15）
  - 未登录提示文案改为"还没有记录嘛？登录后来打卡 ›"，可点击
  - 主色全局替换为 #D93025

## 其他问题处理
- project.config.json 的 appid 从错误值 wxe4f8a2d5c6b9f123 修正为 wx66e13e66977240b1
- 清空了 package.json 的无用依赖（@dcloudio/uni-ui、tdesign-miniprogram）
- manifest.json 已有 lazyCodeLoading: requiredComponents，"组件按需注入"扫描问题需在微信开发者工具手动勾选
- tabBar 图标（static/icon/）被用户误删，必须恢复本地文件，不能用云存储替代
