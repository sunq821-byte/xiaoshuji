# 2026-04-22 工作日志

## 语音导览功能优化

### 优化内容
修改 `pages/heritage/heritageDetail.vue`：

1. **data 新增 `isGenerating` 状态**：防止TTS生成过程中重复点击
2. **toggleAudioGuide 增加判断**：播放中或生成中都直接 return
3. **startAudioGuide 使用 try-finally**：确保 isGenerating 最终重置
4. **playAudioFromUrl 立即 hideLoading**：播放开始即隐藏加载提示

### 页面退出停止播放
已有 `onUnload` 生命周期调用 `stopAudio()`，无需额外修改

### 同步问题
景点详情页 `pages/scenic/scenicDetail.vue` 已同步优化（用户确认）
